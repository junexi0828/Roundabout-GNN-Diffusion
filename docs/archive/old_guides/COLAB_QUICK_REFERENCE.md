# Colab 빠른 참조 가이드

## 🚀 한눈에 보는 전체 프로세스

```
┌─────────────────────────────────────────────────────────────┐
│  [셀 1] 환경 설정                                            │
│  - 라이브러리 설치                                           │
│  - GPU 확인                                                  │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  [셀 2] 프로젝트 파일                                        │
│  - GitHub 클론 또는 업로드                                   │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  [셀 3] 데이터셋                                             │
│  - Google Drive 마운트 또는 업로드                           │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  [셀 4] 데이터 확인                                          │
│  - CSV 파일 확인                                             │
│  - 통계 출력                                                 │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  [셀 5] 데이터 전처리                                        │
│  - 데이터 통합                                               │
│  - 윈도우 생성                                               │
│  - 저장                                                      │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  [셀 6] 데이터 로더                                          │
│  - 데이터 분할                                               │
│  - DataLoader 생성                                           │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  [셀 7] 모델 학습 ⚡                                         │
│  - 모델 생성                                                 │
│  - 학습 실행                                                 │
│  - 체크포인트 저장                                           │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  [셀 8] TensorBoard (별도 탭)                               │
│  - 실시간 모니터링                                           │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  [셀 9] 모델 평가                                            │
│  - 평가 지표 계산                                            │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  [셀 10] 결과 다운로드                                       │
│  - 체크포인트 다운로드                                       │
└─────────────────────────────────────────────────────────────┘
```

## 📝 셀별 실행 코드 (복사해서 사용)

### 셀 1: 환경 설정
```python
!pip install -q torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
!pip install -q torch-geometric torch-geometric-temporal
!pip install -q pandas numpy scipy scikit-learn matplotlib seaborn
!pip install -q opencv-python networkx tqdm pyyaml shapely tensorboard

import torch
print(f"✓ GPU: {torch.cuda.is_available()}")
if torch.cuda.is_available():
    print(f"✓ GPU: {torch.cuda.get_device_name(0)}")
```

### 셀 2: 프로젝트 파일
```python
!git clone https://github.com/your-repo/Roundabout_AI.git
%cd Roundabout_AI
```

### 셀 3: 데이터셋
```python
from google.colab import drive
drive.mount('/content/drive')
import os
os.symlink('/content/drive/MyDrive/Roundabout_AI/data', '/content/Roundabout_AI/data')
```

### 셀 4: 데이터 확인
```python
import sys
sys.path.append('/content/Roundabout_AI')
from pathlib import Path
import pandas as pd

data_dir = Path('/content/Roundabout_AI/data/sdd/converted')
csv_files = sorted(data_dir.glob('*.csv'))
print(f"✓ 데이터 파일: {len(csv_files)}개")
```

### 셀 5: 전처리
```python
from src.data_processing.preprocessor import TrajectoryPreprocessor
import pickle
import numpy as np

# 데이터 통합
all_data = [pd.read_csv(f) for f in csv_files]
combined_df = pd.concat(all_data, ignore_index=True)

# 전처리
preprocessor = TrajectoryPreprocessor(obs_window=30, pred_window=50)
windows = []
for track_id in combined_df['track_id'].unique()[:100]:  # 샘플링
    track_data = combined_df[combined_df['track_id'] == track_id].sort_values('frame_id')
    if len(track_data) >= 80:
        windows.extend(preprocessor.create_sliding_windows(track_data))

# 저장
with open('/content/Roundabout_AI/data/processed/sdd_windows.pkl', 'wb') as f:
    pickle.dump(windows, f)
print(f"✓ 윈도우: {len(windows)}개")
```

### 셀 6: 데이터 로더
```python
from src.training.data_loader import TrajectoryDataset, create_dataloader, split_dataset

train_windows, val_windows, _ = split_dataset(windows, 0.7, 0.15, 0.15)
train_dataset = TrajectoryDataset(train_windows)
val_dataset = TrajectoryDataset(val_windows)

train_loader = create_dataloader(train_dataset, batch_size=32, shuffle=True)
val_loader = create_dataloader(val_dataset, batch_size=32, shuffle=False)
```

### 셀 7: 학습
```python
import torch
from src.models.a3tgcn_model import create_a3tgcn_model
from src.training.trainer import create_trainer

device = torch.device('cuda')
model = create_a3tgcn_model(node_features=9, hidden_channels=64, pred_steps=50)

config = {
    'optimizer': 'adam', 'learning_rate': 0.001,
    'num_epochs': 50, 'log_dir': '/content/Roundabout_AI/runs',
    'save_dir': '/content/Roundabout_AI/checkpoints'
}

trainer = create_trainer(model, train_loader, val_loader, config)
trainer.train(50)
```

### 셀 8: TensorBoard
```python
%load_ext tensorboard
%tensorboard --logdir /content/Roundabout_AI/runs
```

### 셀 9: 평가
```python
from src.evaluation.evaluator import ModelEvaluator
evaluator = ModelEvaluator(model, device)
metrics = evaluator.evaluate_dataset(val_loader)
print(metrics)
```

### 셀 10: 다운로드
```python
from google.colab import files
files.download('/content/Roundabout_AI/checkpoints/best_model.pth')
```

## ⚡ 빠른 실행 (통합 스크립트)

```python
# 모든 단계를 한 번에 실행
!python /content/Roundabout_AI/scripts/colab_full_pipeline.py \
    --data_dir /content/Roundabout_AI/data/sdd/converted \
    --batch_size 32 \
    --epochs 30 \
    --sample_ratio 0.3
```

## 📊 진행 상황 확인

- **TensorBoard**: 셀 8 실행 후 별도 탭에서 확인
- **학습 진행**: 셀 7에서 실시간 출력
- **체크포인트**: `/content/Roundabout_AI/checkpoints/` 확인

## 🔄 병렬 실행 팁

1. **Colab**: Phase 4 (학습) 집중
2. **로컬**: Phase 2-3 (데이터 준비) 가능
3. **결과 공유**: Google Drive 또는 GitHub

